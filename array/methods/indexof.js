// search array and return index
const numbers = [1, 2, 3, 4, 5];

const index = numbers.indexOf(3);

console.log(index);
// Output:
// 2

// If the element is not found, it returns -1
const notFoundIndex = numbers.indexOf(6);

console.log(notFoundIndex);
// Output:
// -1
